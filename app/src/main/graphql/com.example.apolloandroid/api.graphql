query findAvailableAuthors{
  findAllAuthors{
    firstName
    lastName
  }
}

query findAvailableBooks{
  findAllBooks{
    title
    author {
      firstName
      lastName
    }
    pageCount
    isbn
  }
}